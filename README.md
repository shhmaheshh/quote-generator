# Quote Generator With History

A Flask application that fetches random quotes from an external API and stores them in SQLite.

## Features

- Generate random quotes
- Store quotes in database
- Display quote history
- Flask backend
- HTML/CSS/JavaScript frontend

## Installation

```bash
pip install -r requirements.txt
python app.py
```

Visit:

http://127.0.0.1:5000
